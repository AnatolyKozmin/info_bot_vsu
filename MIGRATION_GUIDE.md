# 🔄 Руководство по миграциям

## Что такое миграции?

Миграции создают/обновляют структуру базы данных (таблицы, колонки) согласно моделям в коде.

## 🏗️ Текущая система

У вас используется простой подход через SQLAlchemy:
- Файл `migrate.py` создаёт все таблицы из моделей
- Запускается автоматически при старте контейнера
- Безопасен для повторного запуска (не пересоздаёт существующие таблицы)

## 📋 Текущие таблицы

После миграции будут созданы:
1. ✅ `users` - пользователи бота (НОВАЯ)
2. ✅ `questions` - вопросы от пользователей
3. ✅ `faq` - база знаний
4. ✅ `broadcast_interactions` - взаимодействия с рассылками (НОВАЯ)

## 🚀 Как применить миграции

### Способ 1: Автоматически при перезапуске (рекомендуется)

```bash
# 1. Остановить контейнеры
docker-compose down

# 2. Пересобрать образ с новым кодом
docker-compose build

# 3. Запустить (миграции применятся автоматически)
docker-compose up -d

# 4. Проверить логи
docker-compose logs -f bot
```

**Что вы увидите в логах:**
```
db_1   | database system is ready to accept connections
bot_1  | Ожидание запуска PostgreSQL...
bot_1  | Применяем миграции...
bot_1  | ✅ Миграции успешно применены!
bot_1  | 📋 Таблицы: users, questions, faq, broadcast_interactions
bot_1  | Запуск бота...
bot_1  | Работает
```

### Способ 2: Вручную в работающем контейнере

Если контейнер уже запущен и вы хотите применить миграции без перезапуска:

```bash
# Выполнить миграции вручную
docker-compose exec bot python migrate.py
```

### Способ 3: Локально (без Docker)

Если вы тестируете локально:

```bash
# Убедитесь, что PostgreSQL запущен локально
# и .env настроен на DB_URL (не DOCKER_DB_URL)

python migrate.py
```

## 🔍 Проверка результата

### Проверить через Docker:

```bash
# Подключиться к PostgreSQL в контейнере
docker-compose exec db psql -U vshu -d vshu_db

# Посмотреть список таблиц
\dt

# Должны увидеть:
#  public | broadcast_interactions | table | vshu
#  public | faq                    | table | vshu
#  public | questions              | table | vshu
#  public | users                  | table | vshu

# Выйти
\q
```

### Проверить структуру таблицы users:

```bash
docker-compose exec db psql -U vshu -d vshu_db -c "\d users"
```

Должны увидеть:
```
Column     |            Type             | Nullable | Default
-----------+-----------------------------+----------+---------
id         | integer                     | not null | nextval(...)
tg_id      | bigint                      | not null |
username   | character varying           |          |
first_name | character varying           |          |
last_name  | character varying           |          |
created_at | timestamp without time zone |          |
updated_at | timestamp without time zone |          |
is_active  | boolean                     |          |
```

## ⚠️ Важные замечания

### 1. Безопасность
✅ `create_all()` НЕ удаляет данные из существующих таблиц  
✅ НЕ пересоздаёт существующие таблицы  
✅ Создаёт ТОЛЬКО новые таблицы/колонки  

### 2. Ограничения текущего подхода

❌ **Не поддерживает:**
- Изменение типа колонки (нужно вручную)
- Удаление колонки (нужно вручную)
- Переименование таблицы (нужно вручную)

✅ **Поддерживает:**
- Создание новых таблиц
- Добавление новых колонок
- Создание индексов

### 3. Для production (в будущем)

Для более сложных миграций рекомендуется использовать Alembic:

```bash
# Установить Alembic
pip install alembic

# Инициализировать
alembic init alembic

# Создать автомиграцию
alembic revision --autogenerate -m "Add users table"

# Применить
alembic upgrade head
```

## 🐛 Решение проблем

### Проблема: Таблица не создалась

**Решение:**
```bash
# 1. Проверьте логи
docker-compose logs bot

# 2. Проверьте подключение к БД
docker-compose exec bot python -c "from database.engine import engine; print('OK')"

# 3. Запустите миграцию вручную
docker-compose exec bot python migrate.py
```

### Проблема: Ошибка "relation already exists"

**Причина:** Таблица уже существует (это нормально)

**Решение:** Ничего делать не нужно, это предупреждение можно игнорировать

### Проблема: Новая колонка не появилась

**Причина:** SQLAlchemy `create_all()` не добавляет колонки в существующие таблицы

**Решение:** Добавить колонку вручную через SQL:

```bash
docker-compose exec db psql -U vshu -d vshu_db

# Пример добавления колонки
ALTER TABLE users ADD COLUMN new_column VARCHAR;

\q
```

## 📊 Проверка данных

### Проверить количество пользователей:

```bash
docker-compose exec db psql -U vshu -d vshu_db -c "SELECT COUNT(*) FROM users;"
```

### Посмотреть последних 5 пользователей:

```bash
docker-compose exec db psql -U vshu -d vshu_db -c "SELECT tg_id, username, created_at FROM users ORDER BY created_at DESC LIMIT 5;"
```

### Проверить взаимодействия с рассылками:

```bash
docker-compose exec db psql -U vshu -d vshu_db -c "SELECT broadcast_id, COUNT(*) as reads FROM broadcast_interactions GROUP BY broadcast_id;"
```

## 🔄 Workflow для обновлений

При добавлении новых таблиц в `database/models.py`:

1. ✅ Остановить контейнер: `docker-compose down`
2. ✅ Пересобрать: `docker-compose build`
3. ✅ Запустить: `docker-compose up -d`
4. ✅ Проверить логи: `docker-compose logs -f bot`
5. ✅ Убедиться что миграция прошла успешно
6. ✅ Протестировать новый функционал

## 📝 Резюме

**Для применения новых таблиц (users и broadcast_interactions):**

```bash
docker-compose down
docker-compose build
docker-compose up -d
docker-compose logs -f bot
```

Готово! ✅

