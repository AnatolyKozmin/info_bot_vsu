# ⚡ Быстрый старт

## 🚀 Применить обновления и миграции

```bash
# Полный перезапуск с миграциями (рекомендуется)
docker-compose down && docker-compose build && docker-compose up -d

# Смотреть логи в реальном времени
docker-compose logs -f bot
```

## ✅ Проверить что всё работает

```bash
# 1. Проверить статус контейнеров
docker-compose ps

# 2. Проверить логи бота
docker-compose logs bot | grep "Работает"

# 3. Проверить что таблицы созданы
docker-compose exec db psql -U vshu -d vshu_db -c "\dt"
```

## 🧪 Тестирование новых функций

### 1. Регистрация пользователей
- Откройте бота в Telegram
- Нажмите `/start`
- Вы автоматически зарегистрируетесь в БД

### 2. Статистика
- Отправьте боту: `/stats`
- Увидите себя в списке пользователей

### 3. Рассылка
```
1. /rass
2. Выберите тип контента
3. Отправьте контент
4. Выберите: Да (с кнопкой отслеживания)
5. Выберите: Тестовая рассылка
6. Подтвердите
7. Нажмите кнопку "Прочитал(-а)"
8. Используйте /bstats <ID> для проверки
```

## 🛠️ Полезные команды

### Управление контейнерами
```bash
# Запустить
docker-compose up -d

# Остановить
docker-compose down

# Перезапустить
docker-compose restart

# Пересобрать и запустить
docker-compose up -d --build
```

### Логи
```bash
# Все логи
docker-compose logs

# Только бот
docker-compose logs bot

# Следить в реальном времени
docker-compose logs -f bot

# Последние 50 строк
docker-compose logs --tail=50 bot
```

### База данных
```bash
# Подключиться к PostgreSQL
docker-compose exec db psql -U vshu -d vshu_db

# Посмотреть таблицы
docker-compose exec db psql -U vshu -d vshu_db -c "\dt"

# Посмотреть пользователей
docker-compose exec db psql -U vshu -d vshu_db -c "SELECT * FROM users LIMIT 5;"

# Посмотреть статистику рассылок
docker-compose exec db psql -U vshu -d vshu_db -c "SELECT broadcast_id, COUNT(*) FROM broadcast_interactions GROUP BY broadcast_id;"
```

### Миграции
```bash
# Применить миграции вручную (если контейнер запущен)
docker-compose exec bot python migrate.py

# Применить с перезапуском (автоматически)
docker-compose down && docker-compose up -d
```

### Отладка
```bash
# Зайти внутрь контейнера бота
docker-compose exec bot bash

# Проверить Python версию
docker-compose exec bot python --version

# Проверить установленные пакеты
docker-compose exec bot pip list

# Запустить Python внутри контейнера
docker-compose exec bot python
```

## 🔧 Первый запуск

```bash
# 1. Клонировать проект (если ещё не сделано)
cd путь/к/проекту

# 2. Создать .env файл (если нет)
# Скопировать .env.example и заполнить своими данными

# 3. Запустить
docker-compose up -d

# 4. Проверить логи
docker-compose logs -f

# 5. Дождаться сообщения "Работает"
```

## 📊 Проверка после обновления

### Проверить новые таблицы:
```bash
docker-compose exec db psql -U vshu -d vshu_db -c "
SELECT 
    table_name,
    (SELECT COUNT(*) FROM information_schema.columns WHERE table_name = t.table_name) as column_count
FROM information_schema.tables t
WHERE table_schema = 'public' AND table_type = 'BASE TABLE'
ORDER BY table_name;
"
```

**Должны увидеть:**
```
table_name            | column_count
----------------------+-------------
broadcast_interactions| 4
faq                   | 3
questions             | 8
users                 | 8
```

## 🆘 Если что-то пошло не так

### Полная перезагрузка:
```bash
# Остановить всё
docker-compose down

# Удалить volumes (ВНИМАНИЕ: удалит данные БД!)
docker-compose down -v

# Пересобрать с нуля
docker-compose build --no-cache

# Запустить
docker-compose up -d

# Проверить
docker-compose logs -f bot
```

### Проверить подключение к БД:
```bash
docker-compose exec bot python -c "
from database.engine import engine
import asyncio
async def test():
    async with engine.begin() as conn:
        print('✅ Подключение к БД успешно!')
asyncio.run(test())
"
```

## 📚 Документация

- `MIGRATION_GUIDE.md` - подробно про миграции
- `BROADCAST_GUIDE.md` - руководство по рассылкам
- `ADMIN_COMMANDS.md` - шпаргалка команд админа
- `CHANGELOG.md` - список изменений

## ✅ Чеклист обновления

- [ ] Остановлен контейнер: `docker-compose down`
- [ ] Пересобран образ: `docker-compose build`
- [ ] Запущен: `docker-compose up -d`
- [ ] Проверены логи: `docker-compose logs -f bot`
- [ ] Видно "Миграции успешно применены!"
- [ ] Видно "Работает"
- [ ] Проверены таблицы: `\dt` показывает users и broadcast_interactions
- [ ] Протестирован `/start`
- [ ] Протестирован `/stats`
- [ ] Протестирован `/rass`

Всё готово! 🎉

